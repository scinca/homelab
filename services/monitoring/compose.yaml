services:
  grafana:
    image: grafana/grafana:13.1.1
    container_name: grafana
    restart: unless-stopped
    networks:
      - frontend
    labels: # hostname is specified by default rule with container_name
      - "traefik.enable=true"
      - "traefik.http.routers.grafana.rule=Host(`grafana.scinca.dev`)"
      - "traefik.http.services.grafana.loadbalancer.server.port=3000"
    volumes:
      - grafana-data:/var/lib/grafana


  prometheus:
     image: prom/prometheus:v3.13.2-distroless
     container_name: prometheus
     restart: unless-stopped
     networks:
       - frontend
     command:
       - --web.enable-remote-write-receiver
       - --storage.tsdb.path=/prometheus
       - --config.file=/etc/prometheus/prometheus.yml
     volumes:
        - ./prometheus-config.yaml:/etc/prometheus/prometheus.yml:ro
        - prometheus-data:/prometheus

  node_exporter:
    image: prom/node-exporter:v1.12.0-distroless
    container_name: node_exporter
    restart: unless-stopped
    networks:
      - frontend
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/host:ro
    command:
      - "--path.procfs=/host/proc"
      - "--path.sysfs=/host/sys"
      - "--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)"

volumes:
  prometheus-data:
    name: prometheus-data
  grafana-data:
    name: grafana-data

networks:
  frontend:
    external: true

